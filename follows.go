package twitcher

// UsersFollowing returns an array of users and an error for all users that
// are follow the user `id`
func (c Client) UsersFollowing(id string) (users []User, err error) {
	return
}

// FollowedByUser returns an array of users and an error for all users that
// are followed by the user `id`
func (c Client) FollowedByUser(id string) (users []User, err error) {
	return
}
